# Agent Memory Rule

Memory protocol lives in the **persistent-memory** skill (the canonical, comprehensive source):

- Skill source: `skills/foundation/persistent-memory/SKILL.md`
- Flat symlink: `$skill{persistent-memory}`

This rule exists only as a backwards-compatible pointer — load the skill for the full protocol.
