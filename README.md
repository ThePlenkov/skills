# skills

Personal, agent-agnostic skills repository.

## Layout
- `.agents/skills/` contains all skills.
- `AGENTS.md` documents repo conventions for agents.

## Quick start
1. Create a new folder under `.agents/skills/`.
2. Add `SKILL.md` to that folder.
3. List the skill in `.agents/skills/README.md`.
