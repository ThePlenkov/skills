# Related skills

Skills this skill may delegate to or be used alongside:

- act — when the issue is review-thread remediation on a PR/MR you own.
- blame-trace — find the commit that introduced the suspected code.
- external-tools — GitLab and GitHub CLI patterns for issues, runs, and PRs/MRs.
- github-pr-review — when the user explicitly wants a code review rather than triage.
- mr-description-writer — for the PR/MR body after the fix is pushed.
- mr-review-buddy — for the PR/MR review after the fix is pushed.
- split-draft-mrs — when the fix is large enough that stacked draft MRs make sense.
