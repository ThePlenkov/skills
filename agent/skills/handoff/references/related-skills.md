# Related skills

- `shared-plan` — when the next session is one of many agents continuing a long plan.
- `evidence` — when a claim in the handoff must be proven.
- `review-methodology` — before a merge.
- `unwind` — collapse a solved subtask into the parent plan instead of forking a new session.
