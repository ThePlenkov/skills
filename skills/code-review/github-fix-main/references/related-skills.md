# Related skills

Skills this skill may delegate to or be used alongside:

- external-tools — raw `gh` and `glab` patterns for API calls.
- github-pr-review — reviewer side once the PR is open.
- git-workflow-and-versioning — atomic commit and push conventions.
- triage-issue — when the incoming ask is issue triage rather than a broken main branch.
