# Related skills

- `external-research` — catalog of native search engines and chat tools for foreign code and data.
- `investigate-first` — gather evidence before scoping, especially for the current project's own code.
- `reuse-first` — search locally and in open-source for existing solutions before writing new code.
- `dep-cost` — judge whether a dependency is worth its cost for the surface actually used.
- `minimal-root-cause` — avoid speculative or over-engineered solutions; the underlying philosophy.
