# OpenAI Codex CLI

Source: <https://developers.openai.com/codex/cli/slash-commands>

## Reserved slash commands

- `/skills`

## Notes

Skills are loaded from `~/.codex/skills/` and project `.codex/skills/`. In the TUI, use `/skills` to browse skills or `$skill-name` to mention a skill directly.
