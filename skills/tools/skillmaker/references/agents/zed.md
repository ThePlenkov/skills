# Zed

Source: <https://zed.dev/docs/ai/skills>

## Reserved slash commands

- `/create-skill`

## Notes

Skills are loaded from `~/.agents/skills/` and project `.agents/skills/`. Any installed skill can be invoked with `/skill-name`; the built-in `/create-skill` command creates skills.
