The workspace contains a small Python package with a failing test. Run the tests, investigate why `test_greet` is failing, and write your findings to `/workspace/investigation_report.md`.

Do NOT edit any source files (`src/greet.py`, `tests/test_greet.py`, or any other `.py` file).

Your report must include these sections:
- Investigation target
- Evidence found
- Likely cause
- Files relevant to patch
- Suggested next step
- Commands run
