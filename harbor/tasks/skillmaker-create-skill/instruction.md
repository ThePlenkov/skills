Create a new agent skill named `test-http-check` in the `skills/testing/` directory of the workspace. The skill should teach an agent how to check whether an HTTP endpoint is reachable and report its HTTP status code.

Follow the repository conventions:
- Create `skills/testing/test-http-check/SKILL.md`
- Use valid YAML frontmatter with `name`, `description`, and `allowed-tools`
- Keep the name in kebab-case and make sure it does not conflict with any reserved name listed in `scripts/reserved-names.sh`
- Include a short procedure in the body

Do not modify any existing files.
